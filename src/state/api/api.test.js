import configureStore from 'redux-mock-store';

const middleware = [];
const mockStore = configureStore(middleware);

describe('If a resource already exists in the cache a fetch will not be called', () => {
    it('ADD TESTS!', () => {})
})
